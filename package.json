{
  "name": "device-service",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "//1": "--------------------------- Dev for Lib -----------------------------------------",
    "run_lib_build": "clear && cargo build --lib",
    "start-lib": "nodemon --watch my_macro_lib/src --ext rs --exec 'npm run run_lib_build'",

    "//2": "--------------------------- Dev for Consumer -------------------------------------",
    "run_consumer_build": "clear && cargo run --bin my_macro_consumer",
    "start-consumer": "nodemon --watch my_macro_consumer/src --ext rs --exec 'npm run run_consumer_build'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2",
    "npm-run-all": "^4.1.5"
  }
}
